/* frontend/live-config.js
 * Live API origin for GitHub Pages / Vercel static apps.
 *
 * After Cloudflare Workers deploy, set this to your Worker URL, e.g.:
 *   window.POWERBULL_LIVE_API = 'https://mayankdoshi.<subdomain>.workers.dev';
 *
 * Or enter it once in More → Dhan API → “Live API host” (saved in this browser).
 * Never put the Dhan access token in the URL.
 */
window.POWERBULL_LIVE_API = window.POWERBULL_LIVE_API || '';
